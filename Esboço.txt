 

  PONTO DE PARTIDA: Linec (7 arquivos -> main.asm + 6 módulos) 
 
  - O trabalho possivelmente terá bem mais...

  -> Cada função terá um módulo. Posteriormente, todos serão reunidos em main.asm.



  SEPARAÇÕES DOS MÓDULOS:

   - main.asm (irá reunir os módulos, as variáveis e os segmentos da memória);

   - cursor.asm (construtor do cursor);

   - caracter.asm (construtor de caracteres);

   - fcircle.asm (construtor de círculos, construirá os círculos da bolinha e a sua interface);

    -line.asm (configurações gerais das linhas, será usado para fazer a raquete);

   - plot_xy.asm (constrói o plot XY da interface);

   - testKey (testa a tecla via interrupção do hardware)

   - colisao.asm (Tratamento de colisões);

   - animacao.asm (loop principal - animação da raquete e da bolinha);